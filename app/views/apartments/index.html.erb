<p id="notice"><%= notice %></p>

<h1>Listing Apartments</h1>

<table>
  <thead>
    <tr>
      <th>Street1</th>
      <th>Street2</th>
      <th>City</th>
      <th>Postal</th>
      <th>State</th>
      <th>Country</th>
      <th>Contact name</th>
      <th>Phone</th>
      <th>Hours</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @apartments.each do |apartment| %>
      <tr>
        <td><%= apartment.street1 %></td>
        <td><%= apartment.street2 %></td>
        <td><%= apartment.city %></td>
        <td><%= apartment.postal %></td>
        <td><%= apartment.state %></td>
        <td><%= apartment.country %></td>
        <td><%= apartment.contact_name %></td>
        <td><%= apartment.phone %></td>
        <td><%= apartment.hours %></td>
        <td><%= apartment.user.email %></td>
        <td><%= link_to 'Show', apartment %></td>
        <% if current_user == apartment.user %>
          <td><%= link_to 'Edit', edit_apartment_path(apartment) %></td>
          <td><%= link_to 'Destroy', apartment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Apartment', new_apartment_path %>

<br></br>
<!--took out instance variable for finding a specific apartment ID because we want all of the locations to show up on the map  -->
<div id="map_location_all">Google Map will appear here...</div>
